name: 'Docker Image Build, Test, Scan, and Publish'

runs:
  using: "composite"
  steps:
    - name: Debug
      shell: bash
      run: |
        echo "${{ github.action_path }}"
        ls -ltr "${{ github.action_path }}"
        cd "${{ github.action_path }}"
        cat "${{ github.action_path }}"/../common.properties
    - name: Debug
      uses: "${{ github.action_path }}"/../validate-inputs